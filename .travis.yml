notifications:
  email: false
git:
  depth: 10
os: osx

before_install:
  - time travis_retry brew install --force-bottle --build-bottle wine || true
  - export DISPLAY=:1
  - time wineboot

script:
  - echo Done.
